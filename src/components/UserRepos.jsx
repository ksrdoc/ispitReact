import React from "react";
import PropTypes from "prop-types";

const UserRepos = () => {
  return (
    <div>
      <h2>User Repos</h2>
      <ul id="reposList"></ul>
    </div>
  );
};

//Nije se stiglo nazalost

UserRepos.propTypes = {};

export default UserRepos;
